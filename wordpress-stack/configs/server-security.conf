# Security headers
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload";
add_header X-Content-Type-Options nosniff;
add_header X-XSS-Protection "1; mode=block" always;
add_header Referrer-Policy "origin" always; # or "strict-origin" always;

# add_header Content-Security-Policy "default-src 'self' *.$host;img-src https://* data: blob:; child-src 'none';script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; font-src 'self' data:;";
# add_header Content-Security-Policy "default-src 'self'; style-src 'self' https://cdn.example.com";

###
# add_header 'Access-Control-Allow-Origin' 'https://foo.example' always;
# add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS' always;
# add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range' always;
###

###
# add_header Permissions-Policy "geolocation=(),midi=(),sync-xhr=(),microphone=(),camera=(),magnetometer=(),gyroscope=(),fullscreen=(self),payment=()";
# add_header X-Frame-Options DENY;
# add_header X-Frame-Options "SAMEORIGIN";
# add_header X-Frame-Options "ALLOW-FROM https://example.com" always;
###s